# Fourth-pass capability status

**Last reviewed:** 2026-08-09
**Baseline commit:** `10896e19aa2a2151edff1bf550f0b3e56fcfe27e`

This is a source and verification ledger, not a deployment record. A source
change, test result, or merged commit does not claim that the capability is
released or operator-verified. All production columns remain `No` until a
separately approved release and post-release verification occur.

| Finding or capability | Source implementation status | Unit/integration coverage | Browser coverage | Compose/fault-injection coverage | Migration required | Production release status | Operator verification status | Remaining owner gate | Last reviewed |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Terminal feed and podcast refresh jobs release their deterministic source IDs | Implemented in source; terminal source jobs remove on failure and retain compact failure evidence | Queue-option, readiness, and source-failure tests | Not applicable | Real local Redis lifecycle tests cover terminal failure, re-enqueue after worker restart, and concurrent deduplication | No | No | No | Review and approve a future release | 2026-08-08 |
| Durable Redis heartbeat and maintenance recovery | Implemented in source: shared bounded reconnect control plane, fail-closed lease loss, gated local heartbeat, and nonzero shutdown request after a bounded recovery grace | State, grace, shutdown, heartbeat gating, lease ownership, later-tick, and no-release-after-loss tests | Not applicable | Real disposable Redis restart test passes; Compose topology restart gate is added to CI but awaits CI execution | No | No | No | Review and approve a future release | 2026-08-08 |
| Manual and fallback source refreshes use BullMQ rather than the web process | Implemented in source: add-feed, directory, manual, and source-attention paths authorize then enqueue; OPML retries only when the initial XML import did not complete | Queue contract, action outcomes, OPML no-duplicate fallback, trigger payload, and source-architecture tests | Authenticated browser coverage remains pending | Real Redis lifecycle test confirms source deduplication remains intact | No | No | No | Review and approve a future release | 2026-08-08 |
| Account-deletion handoff rate-limit order | Implemented in source: v2 uses a cached HKDF-derived HMAC, while the confirmation route authenticates and rate-limits before parsing or verifying the handoff | Handoff format, legacy compatibility, input-bound, route-order, replay, and invalid-load tests | Not applicable | No production abuse run; local invalid-load coverage is retained | No | No | No | Review and approve a future release | 2026-08-08 |
| Feed, podcast, XML, and discovery resource budgets | Implemented in source: item, field, aggregate-content, URL, response-time, candidate-count, and shared-deadline bounds; XML entities are inert and parser metrics contain counts only | Two-megabyte/thousand-item, field/external-ID, entity/DOCTYPE, OPML, charset, cancellation, and normal fixture tests | Not applicable | Local cancellation test proves host-slot release; no production abuse run | No | No | No | Review and approve a future release | 2026-08-08 |
| Changed-only feed and podcast persistence | Implemented in source: versioned SHA-256 fingerprints create only new rows, update corrected or legacy-null rows, and skip identical source items | Fingerprint, correction, unchanged, duplicate-input, and lazy-legacy coverage | Not applicable | Disposable PostgreSQL measurement script verifies insert, zero-WAL identical persistence, corrected update, and bounded single-ID lookups | Yes — nullable expand-only fingerprint fields | No | No | Run the local disposable measurement, then review migration evidence and approve a future release | 2026-08-08 |
| Exact service-role environment enforcement | Implemented in source: one manifest registry covers role settings, compatibility aliases, infrastructure credentials, and host deployment inputs; production startup rejects managed values outside its role | Manifest completeness plus per-role injected-secret startup checks | Not applicable | Rendered Compose environments are verified against the same manifest | No | No | No | Review source support before the owner-gated credential/network rollout | 2026-08-08 |
| Durable-only worker startup isolation | Implemented in Compose: ingestion, AI/mail, imports, and maintenance wait only for migration completion and durable Redis; all-in-one and chat-event workers retain both Redis dependencies | Not applicable | Not applicable | Rendered Compose dependency test proves ephemeral Redis is absent from durable-only worker dependencies | No | No | No | Review source support before the owner-gated credential/network rollout | 2026-08-08 |
| Separate Redis ACL credentials and networks | Implemented in source: durable and ephemeral Redis use distinct ACL users/passwords with the default user disabled; normal production rejects shared endpoint, username, or password. Compose separates durable-data, ephemeral-realtime, and web-edge access | Configuration guard tests cover required/distinct direct URLs | Not applicable | Rendered Compose network check plus disposable ACL Redis integration proves cross-credential rejection and the gateway/ingestion network denials | No | No | No | Owner-gated credential rollout, immutable release, and post-release verification | 2026-08-08 |
| Restricted chat gateway database role | Implemented in source: chat-gateway receives `CHAT_DATABASE_URL` only; idempotent owner bootstrap grants its exact authorization, room, normal-message, read-marker, and event-outbox queries without schema ownership | Gateway security configuration, room/outbox, and manifest tests cover the source boundary | Not applicable | Disposable PostgreSQL test applies the bootstrap twice and proves required queries work while article bodies, account/token and AI data, plan/role updates, reports, and DDL are denied | No | No | No | Owner-gated credential rollout, immutable release, and post-release verification | 2026-08-08 |
| Narrow deterministic story-signal evaluation | Implemented in source: selected and bounded related-coverage candidates use the five-field `StorySignalArticle` projection under the existing subscription/archive guard | Query-shape, explicit canonical, tracker normalization, unsafe-canonical, lookalike-domain, and no-body-read tests cover the boundary | Not applicable | No new source fetch is added; approved linked-page enrichment supplies canonical metadata only when already fetched | No | No | No | Review and approve a future release | 2026-08-08 |
| Current story versions selected before limiting | Implemented in source: active story clusters are queried first and only each highest/current version is selected before the bounded cluster result set | Current-root query shape plus 13-version-history, active/current, paused/unsubscribed/archived, dismissed, and visible-citation tests cover the boundary | Story-analysis copy distinguishes generation and currently visible source counts | No migration; existing visible-member rehydration prevents stale source exposure | No | No | No | Review and approve a future release | 2026-08-08 |
| Shared worker-produced public health snapshot | Implemented in source: dedicated `worker-health` uses the durable maintenance lease, runs deep checks every 20 seconds, and writes a compact versioned durable-Redis snapshot; public health performs only a bounded snapshot read and protected health remains an explicit admin diagnostic | Snapshot publication/TTL/freshness/deadline and 1,000-read tests; route authorization and system-health tests | Not applicable | All topology manifests require the health worker; rendered Compose environment, network, dependency, and topology validation cover its reviewed boundary | No | No | No | Run selected-topology Compose and public/admin health checks after an approved release | 2026-08-08 |
| Structured backup evidence | Implemented in source: a versioned record binds a backup ID, expected production/database identity, timestamp, relative artifact, byte count, SHA-256, off-host acknowledgement, and restore-drill time | Filesystem fixtures cover valid, malformed, unsupported, future, stale, wrong-identity, off-host, restore, artifact, checksum, and traversal outcomes | Not applicable | Windows sync records the remote acknowledgement only after local checksum verification; installation and restricted sudo setup remain operator work | No | No | No | Install the helpers, configure the private policy, run a real backup/sync/restore drill, then approve a future release | 2026-08-08 |
| Migration risk evidence bound to SQL | Implemented in source: every selected new migration requires a fixed record whose name and SHA-256 must match its committed SQL; advisory regex findings remain visible but never substitute for the record | Unit coverage includes required fields, hash/name mismatch, non-ready historical records, forbidden ready-state language, approval timestamp, and backup-evidence-ID requirement | Not applicable | CI and approved release invoke the same verifier before Prisma; production measurements and approval are deliberately absent here | No | No | No | Capture current measured evidence, a fresh backup evidence ID, and explicit approval before marking any record ready | 2026-08-08 |
| One-active OPML import per user | Implemented in source: PostgreSQL partial unique index permits one `PENDING` or `PROCESSING` job per user, and the application maps a constraint conflict to the existing active-import message | OPML create, failed-enqueue, and retry-conflict tests | Not applicable | Disposable PostgreSQL test proves same-process and separate-client contention, terminal transitions, retry, and user isolation | Yes — reviewed partial unique index | No | No | Migration evidence and release approval | 2026-08-08 |
| Source orphan lifecycle reporting | Implemented in source: a read-only aggregate report counts currently unreferenced feeds and podcasts plus collection, directory, chat, audit, legal-hold, state, and digest references; no orphan state or deletion is enabled | Report mapping and malformed-result tests | Not applicable | Not applicable: no mutation or background purge is introduced | No | No | No | Separate owner approval for future marking and bounded purge | 2026-08-08 |
| Current story versions selected before limiting | Baseline finding confirmed; historical versions are limited before current-version filtering | Existing reader tests do not cover historical crowd-out | Not applicable | Not applicable | No | No | No | Review and approve a future release | 2026-08-08 |
| Collection-retained article authorization | Implemented in source: an article is accessible through an active subscription or a collection item owned by that reader; ordinary lists/searches still require an active source, while collection-scoped views/searches retain deliberate saves | Detail, list hydration, collection list, collection search, state mutation, related-story presentation, and foreign-collection query-shape tests | Authenticated subscribe/save/unsubscribe/collection/detail/search/state/remove journey is checked in; execution requires the disposable authenticated fixture | No orphan-article cleanup currently exists; source unsubscribe removes only the subscription and collection references remain until their item is removed | No | No | No | Review the authenticated fixture result, then approve a future release | 2026-08-08 |
| Phase 11A source hygiene | Implemented in source: safe redirect and feed self-link evidence, prior/current URL display, duplicate warning, reviewed recovery, and a typed-confirmation replacement that preserves the existing subscription record, folder, and custom title | Redirect, XML metadata, refresh recovery, subscription ownership/duplicate, action confirmation, and attention rendering tests; full suite passes | No dedicated authenticated browser journey in this run | Not applicable: no worker, queue family, or Compose topology is added | Yes — ten nullable, expand-only `Feed`/`FeedSubscription` fields | No | No | Run the authenticated browser journey; capture fresh production table/lock/backup evidence; then require a new `DEPLOY <short-sha>` approval | 2026-08-09 |

## Baseline evidence index

- Full locked local verification and the real-Redis queue reproduction are
  recorded in [fourth-pass-baseline.md](fourth-pass-baseline.md).
- The third-pass evidence remains in
  [third-pass-capability-status.md](third-pass-capability-status.md); it is
  independent evidence and does not close a fourth-pass finding.
- Production release and rollback rules remain owner-gated in
  [the CI/CD release gate](../operations/ci-cd-release-gate.md) and
  [the deployment rollback runbook](../operations/deployment-rollback-runbook.md).
