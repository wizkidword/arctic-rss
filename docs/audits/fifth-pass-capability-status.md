# Fifth-pass capability status

**Last source review:** 2026-08-10
**Baseline:** `686cd18b7e7f6196865af34c93496c3bddf16a69`  
**Production status:** `c7be850` was deployed and independently verified on
2026-08-10 through the approved OVH release controller.

## Release addendum

The release controller passed the exact-commit CI and local gates, fresh backup
gate, migration ownership preflight, migration status, selected-service health,
loopback health/liveness, public health/login, and monitor checks. Independent
verification confirmed all 51 migrations, the eight required
`all-in-one-with-chat` services, and the private release record.

The per-finding production and operator columns below are the preserved
pre-release implementation snapshot. Their `Not deployed` and `No` entries are
superseded by this release-level evidence; they do not claim that each product
flow was exercised with a real production account.

## Phase 10 mobile platform addendum

After the owner recorded `APPROVE MOBILE PLATFORM ADR`, Phase 10 established
the accepted first-party mobile platform ADR, a shared Zod/TypeScript contract
package, generated OpenAPI 3.1 artifact, and private read-only `/api/v1`
surface. Its ten endpoints use existing fresh-user authorization and
user-owned domain queries, `private, no-store` responses, bounded cursor pages
where list cardinality is not product-limited, request IDs, rate-limit
fail-closed behavior, and low-cardinality telemetry.
List DTOs exclude article bodies; the selected article detail is sanitized.

This is source complete and not deployed. No native client, PKCE exchange,
device session, sync mutation, push installation, or Android build exists yet.
Phase 11 is the next source implementation phase; Phase 13 remains separately
gated by `APPROVE ANDROID INTERNAL ALPHA`.

| Finding ID | Source implementation                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Unit/integration coverage                                                                                                                                                                                                                                                                                                                                                        | Browser evidence                      | Redis/PostgreSQL/Compose evidence                                                                                                                                                                                                                                    | Migration required                                                                               | Production release status | Operator verification | Mobile dependency                            | Remaining owner gate                                                                   |
| ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------ | ------------------------- | --------------------- | -------------------------------------------- | -------------------------------------------------------------------------------------- |
| FP-001     | Current-record eligibility now gates monitors, Smart Digest processing/email, AI-digest provider calls, and OPML worker entry processing. Disablement pauses rules, cancels retryable Smart Digest and AI-digest work, and requests cancellation of active OPML imports. Chat bot delivery is room-owned system automation (`senderUserId: null`), so no account-owner eligibility check applies.                                                                                                                         | Policy plus disable-before-claim, disable-during-processing, Smart Digest canceled-run, AI-digest pre-provider recheck, and OPML disabled-owner coverage; full suite passes (286 files, 1,406 tests)                                                                                                                                                                             | Not run this pass                     | All 45 migrations applied to a disposable PostgreSQL 17.10 fixture; both `CANCELED` values, OPML `PROCESSING`, and the active-claim index were verified.                                                                                                             | Yes — additive enum values and OPML lease metadata; production ready false                       | Not deployed              | No                    | Required before device sessions              | Continue Phase 2 replay/Redis verification; release remains `DEPLOY <short-sha>` gated |
| FP-002     | Entry processing is fenced with expand-only lease fields, `PROCESSING`, atomic `FOR UPDATE SKIP LOCKED` claims, attempt/owner/expiry checks, and parent counters updated only by a successful fenced terminal transition. Completion requires no pending or processing entries.                                                                                                                                                                                                                                           | Lease unit tests; PostgreSQL concurrent-claim, reclaim, stale-finalization, cancellation, counter, and completion checks; real Redis/BullMQ duplicate-delivery; separate-process reclaim; and a real BullMQ stalled-lock injection. The stalled worker finishes simulated external work, loses its Redis lock, and is rejected after the replacement worker finalizes attempt 2. | Not run                               | All 45 migrations applied to a disposable PostgreSQL 17.10 fixture; OPML enum and active-claim index verified. Duplicate-delivery and stalled-lock coverage ran against disposable PostgreSQL and Redis; separate-process reclaim ran against disposable PostgreSQL. | Yes — enum, lease metadata, and active-claim index; production ready false                       | Not deployed              | No                    | None                                         | Approved release verification                                                          |
| FP-003     | Smart Digest workers claim attempt/owner/expiry leases, renew during candidate selection, and fence recovery, nullable-unique `runId` upsert, watermark advancement, failure, and completion. Disablement cancels in-progress runs and clears their lease; legacy `digestId` remains compatible. SMTP-accepted acknowledgement failures become `DELIVERY_UNKNOWN`. The documented operator command inspects the provider message ID and must explicitly confirm delivery or non-delivery; only the latter queues a retry. | Claim/fence, disabled-owner, SMTP rejection, accepted-and-acknowledged, accepted-and-unacknowledged, no-resend, and reconciliation unit coverage; PostgreSQL stale-worker reclaim proves the replacement creates the only `runId` digest, advances the watermark, and rejects the stale update.                                                                                  | Not run                               | All 47 migrations applied to a disposable PostgreSQL 17.10 fixture. Lease fields, indexes, deferred-validation reverse foreign key, stale-worker fixture, and `DELIVERY_UNKNOWN` enum value were verified.                                                           | Yes — additive lease metadata, nullable `runId`, and delivery enum state; production ready false | Not deployed              | No                    | None                                         | Approved release verification                                                          |
| FP-004     | Queue readiness now excludes feed and podcast publisher failures. A separate internal source-reliability report retains bounded source-id-free terminal outcomes, feed/podcast failure counts, normalized categories, affected hosts, failure percentage, recurring hosts, and a conservative multi-host/high-percentage internal-failure classifier. Health snapshots use a dedicated lock; scheduling, health publication, and orphan reporting have distinct leases. Essential feed/podcast scheduler success ticks determine maintenance freshness; optional work records independent ticks. Orphan reporting is a bounded, timeout-limited, separately scheduled dry-run report. | Source-only failure, shared database failure, non-diverse timeout evidence, queue-lag/stalled-job, individual scheduler-tick, snapshot isolation, lock contention, Redis initial-connect, and orphan timeout coverage; full suite passes (290 files, 1,421 tests; 9 files/13 tests skipped). | Not run | Local mocked Redis/BullMQ evidence only; no production runtime inspection. | No | Not deployed | No | None | Approved release verification |
| FP-005     | Raw external IDs are capped at 2 KiB, while new and normally changed articles/episodes dual-write a 64-character SHA-256 hash of the documented, control-safe, line-ending-normalized identity. A separate resumable command stores per-scope ID checkpoints and count-only collision candidates, but it accepts only an explicit `disposable` confirmation and loopback database URL. Existing raw-ID uniqueness remains authoritative; no index or lookup switch was made. | Parser byte-limit, identity-normalization/fixed-length hash, forced collision seam, new/changed dual-write, resumable checkpoint, and non-loopback refusal coverage; full suite passes (294 files, 1,439 tests; 9 files/13 tests skipped). | Not run | Fresh loopback PostgreSQL 17.10 rehearsals applied all 49 migrations. The expand rehearsal verified both nullable columns; the guarded backfill rehearsal processed synthetic rows one-at-a-time, resumed both checkpoints, and verified three hashes plus two completed progress rows. | Yes — nullable identity fields and isolated progress table; index/switch intentionally deferred | Not deployed | No | None | Production backfill evidence plus explicit owner approval before a separately reviewed index and lookup-switch release |
| FP-006     | RSS/Atom, podcast, OPML, discovery, transcript, and ingestion-fingerprint paths share a publisher-text boundary that removes NUL/invalid XML controls and unpaired surrogates, normalizes line endings, records count-only metadata, and keeps external identifiers out of NFC normalization. Publication dates accept only 1970–9999 UTC and no more than 72 hours future skew; anomalous values are omitted and counted as `invalid`, `out-of-range`, or `future-skew`. | Every disallowed C0 control, tabs/LF/CR, supplementary Unicode, numeric XML entities, parser-boundary integrations, identity-preserving normalization, date boundaries, malformed/far-future source continuation, and count-only parse metrics are covered. | Not run | Local parser/refresh evidence only; no production runtime inspection. | No additional migration for text/date policy | Not deployed | No | None | Approved release verification |
| FP-007     | Feed and podcast workers atomically claim owner/generation leases, renew during remote work, fence every source-health/metadata write, and release only their current lease. New articles and episodes record the source generation; existing items update only from a higher generation or legacy null. Deterministic queue jobs now use BullMQ's atomic duplicate-add path and a persisted internal request marker to return `queued`, `already-active`, or conservative `unavailable`; callers may set only priority and the safe trigger. | Lease claim/reclaim/release coverage; feed generation-one stall then generation-two success/resume and generation-one failure-after-generation-two-success fixtures; article/episode generation conditions; queue outcome, fixed-options, and unavailable coverage; focused suites pass. | Not run | Fresh loopback PostgreSQL 17.10 rehearsal applied all 50 migrations and verified the source-lease/generation columns. Disposable Redis/BullMQ exercised one terminal replay plus concurrent feed and podcast enqueue requests, with one processed job per source. | Yes — source generation defaults/lease fields and nullable item generations; production ready false | Not deployed | No | None | Fresh production lock/runtime evidence and exact `DEPLOY <short-sha>` approval |
| FP-008     | Reader cluster selection now first resolves qualifying IDs through `StoryCluster.currentVersionNumber` joined directly to its immutable current `StoryClusterVersion`, then requires the requested article to be a member of that current version before hydrating it. Historical members cannot qualify a current cluster. | Current-version SQL join and split-history reader regression coverage; focused suite, typecheck, and lint pass. | Not run | Local source/test verification only; no production query capture. | No additional migration | Not deployed | No | None | Exact `DEPLOY <short-sha>` approval after production evidence |
| FP-009     | The global shell receives only the eight `FeedNavigationItem` fields through an explicit map. Rich source observations are loaded only by Source Hygiene, while discovery surfaces receive URL-only projections. Bulk pause/unsubscribe is transactional; retry returns a named outcome for every selected source. Reverse `FeedSubscription.feedId` and `PodcastSubscription.podcastId` indexes are present in a reviewed concurrent-index migration. | Projection boundary, bulk-action atomic/per-item-result, current-cluster, and migration-risk coverage pass; `shell:measure` has local-only configuration tests. | A signed synthetic local session captured actual 10/100/200-subscription Flight payloads, transferred JS, hydration probe, and JS heap. | Disposable PostgreSQL 17.10 applied all 51 migrations. The local benchmark recorded 18 layout-loader queries at every scale; the 200-source Flight payload was 573,340 bytes. Reverse-index rehearsal used a 10,000-row disposable fixture and an index-only scan. | Yes — reviewed reverse-index migration; production ready false | Not deployed | No | Mobile read API depends on stable projection | Fresh production lock/runtime evidence and exact `DEPLOY <short-sha>` approval |
| FP-010     | Durable and ephemeral Redis application users deny administrative/dangerous command categories, retain only BullMQ-required `INFO`, disable the default user, and are separated by endpoint, ACL username, password, and network. The chat gateway rejects irrelevant legacy Redis aliases; all role startup inputs are manifest-checked. Chat Compose CI now starts the gateway with `arctic_chat`, not the PostgreSQL superuser. | Disposable Redis exercises real BullMQ, worker heartbeat/lease, health snapshot, source evidence, rate-limit Lua, presence, chat pub/sub, Socket.IO adapter, cross-credential/default-user/admin-command, and network-denial paths. Startup tests inject every ineligible managed variable/alias; dual-role endpoint/username/password checks pass. Chat release gates pass: 18 files, 114 tests. | Not run | Disposable PostgreSQL 17.10 applied all 51 migrations, bootstrapped the restricted chat role twice, proved allowed gateway-domain operations, and denied protected data, privilege changes, and DDL. Compose CI is configured to perform that bootstrap before both chat topologies start. | No additional migration expected | Not deployed | No | Required before mobile API exposure | Fresh production credential/topology evidence, remote CI completion, and exact `DEPLOY <short-sha>` approval |

## Account re-enable behavior

This pass deliberately pauses monitor and Smart Digest rules on disablement and
does not provide any automatic reactivation path. If an administrator later
re-enables an account, automation must remain paused until the user or an
explicitly reviewed administrator action opts into each rule again.

## Ledger rules

`FP-001` through `FP-010` are the stable IDs for this pass.  They are
deliberately distinct from release and operator columns: passing local source
evidence does not authorize a production release, and a release cannot be
reported as operator-verified without fresh owner evidence.  Run
`npm run audit:verify-fifth-pass-ledger` to check the IDs, source-evidence
column, and separate production/operator fields.
